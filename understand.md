### 数据库文件
数据库内存文件格式有MANIFEST文件，.log文件，.sst文件，他们分别存储不同的内容  
这三种文件构成了 LevelDB 存储引擎的核心骨架。它们分工明确，共同保证了数据的高效读写和持久化安全。

#### 核心对比概览

| 特性 | LOG 文件 (`.log`) | SST 文件 (`.sst` / `.ldb`) | MANIFEST 文件 (`MANIFEST-xxxx`) |
| :--- | :--- | :--- | :--- |
| **通俗比喻** | 临时记事本（防备忘） | 正式归档的图书（持久查阅） | 图书馆的目录索引（全局统筹） |
| **存储内容** | 尚未落盘的原始操作记录 (Put/Delete) | 排序后的键值对、索引块、布隆过滤器 | SSTable分布、序列号、日志编号等全局元数据 |
| **核心作用** | 故障恢复（Crash Recovery） | 数据的最终持久化存储、高效查询 | 描述数据库当前的宏观状态 |
| **读写特征** | 顺序追加写（Append-only） | 一次性写入，不可变（Immutable） | 顺序追加写（VersionEdit） |
| **创建者** | 主线程（MemTable 切换时） | 后台线程（Flush 或 Compaction 时） | 后台线程 / 数据库启动时 |

---

#### 一、 LOG 文件 (预写式日志 WAL)

**1. 存储的内容：**
* 存储的是**最新的、还在内存中（MemTable）尚未持久化成 SSTable 的数据**。
* 以追加（Append）的方式顺序记录用户的写操作（Put / Delete）。

**2. 核心作用：**
* **防灾恢复（Durability）**。内存中的数据一旦断电就会丢失。写入内存前先记录一条 LOG，如果 LevelDB 突然崩溃，重启时可以通过读取这个 LOG 文件，将数据重新回放（Replay）到内存中，保证数据不丢。

**3. 由谁创建：**
* 当数据库启动，或者当前的内存表（MemTable）写满、系统创建一个新的 MemTable 时，由**前台主写线程**同步创建相应的配套 LOG 文件。

**4. 生命周期（短促）：**
* **生**：伴随新的 MemTable 诞生。
* **死**：当它对应的 MemTable 被后台线程成功“刷盘（Flush）”变成磁盘上的 SST 文件后，这份 LOG 里的数据已经绝对安全了。此时，这个 LOG 文件就会被**删除或归档**。

---

#### 二、 SST 文件 (Sorted String Table 数据文件)

**1. 存储的内容：**
* **核心数据**：经过字典序严格排序的 Key-Value 键值对。
* **索引块（Index Block）**：记录数据块的边界，用于快速二分查找。
* **元数据块（Meta Block / Filter）**：通常是布隆过滤器，用于快速判断某个 Key 是否存在，拦截无效的磁盘读取。
* **Footer**：指向索引块和元数据块位置的固定大小尾部数据。

**2. 核心作用：**
* **持久化存储与高效查询**。这是 LevelDB 存放真正用户数据的地方。内部的高度结构化保证了即使文件很大，也能迅速定位到目标数据。

**3. 由谁创建：**
* **后台合并线程（Compaction Thread）**。具体有两种情况：
  1. Minor Compaction：将写满的 Immutable MemTable 直接“刷”成 Level 0 的 SST 文件。
  2. Major Compaction：将 Level N 的一个或多个 SST 文件与 Level N+1 的文件进行多路归并排序，生成新的 Level N+1 的 SST 文件。

**4. 生命周期（长久但会被替换）：**
* **生**：后台 Flush 或 Compaction 过程结束时生成。一旦生成，**绝对不可变（Immutable）**。
* **死**：当它参与了下一次 Compaction，其包含的有效数据被合并进了更高层级的新 SST 文件中时，这个旧的 SST 文件就会被标记为废弃，随后被**物理删除**。

---

#### 三、 MANIFEST 文件 (版本与元数据描述符)

**1. 存储的内容：**
* **全局宏观状态（VersionEdit）**：
  * 当前哪一层（Level）有哪些 SST 文件。
  * 每个 SST 文件的编号、文件大小、最小 Key（Smallest Key）和最大 Key（Largest Key）。
  * 全局最大的序列号（Sequence Number，用于多版本并发控制）。
  * 下一个可用的文件编号（Next File Number）。
  * 当前正在使用的 LOG 文件编号。

**2. 核心作用：**
* **数据库的大脑与地图**。有了它，LevelDB 启动时才知道一堆乱七八糟的 `.sst` 文件该怎么组织，读请求来了才知道该去哪个文件的哪个区间找数据。

**3. 由谁创建：**
* 数据库第一次创建时，或者当现有的 MANIFEST 文件增长得过大（超过预设阈值）时，系统会创建一个全新的 MANIFEST 文件。每次发生状态改变（如新增或删除了 SST 文件），系统都会把这个变化作为一个 `VersionEdit` 记录追加到 MANIFEST 中。

**4. 生命周期（长久）：**
* **生**：伴随数据库的创建或老 MANIFEST 过大时轮转（Rotate）而生。
* **死**：当生成了新的 MANIFEST 文件，并且系统通过修改 `CURRENT` 文件将其指针指向新 MANIFEST 后，旧的 MANIFEST 文件就会被**废弃并删除**。

---

### **总结：它们是如何协同工作的？**

1. 用户写入一条数据 -> **LOG** 记录下来 -> 数据存入内存。
2. 内存满了 -> 冻结内存数据 -> 后台将内存数据写成静态的 **SST** 文件。
3. **SST** 文件生成后 -> 通知系统更新状态 -> 把新 SST 的信息写入 **MANIFEST** 文件。
4. 状态更新完毕 -> 旧的 **LOG** 文件被无情抛弃，完成一个闭环。

---

### 完整的 SSTable 物理布局图

```text
+----------------------------------------------------------+  <-- 偏移量 0 (文件开头)
|                                                          |
|                     Data Block 1                         |  (存着第一批 KV 数据，带 5 字节尾巴)
|                                                          |
+----------------------------------------------------------+
|                                                          |
|                     Data Block 2                         |  (存着第二批 KV 数据，带 5 字节尾巴)
|                                                          |
+----------------------------------------------------------+
|                        ...                               |
+----------------------------------------------------------+
|                                                          |
|                     Data Block N                         |  (最后一批 KV 数据)
|                                                          |
+==========================================================+  <-- 数据区结束，元数据区开始
|                                                          |
|    +--- [ Filter Block 内部结构展开 ] -----------------+ |
|   |                                                    | |
|   |  Filter Data 1  (Data Block 前2KB数据的布隆数组)   | |
|   |  Filter Data 2  (Data Block 接下2KB数据的布隆数组) | |
|   |  ...                                               | |
|   |  Filter Data M  (Data Block 最后几KB的布隆数组)    | |
|   |  ------------------------------------------------  | |  <-- 过滤器数据结束
|   |  Offset 1 (4 bytes, 指向 Filter Data 1 起点)       | |
|   |  Offset 2 (4 bytes, 指向 Filter Data 2 起点)       | |
|   |  ...                                               | |
|   |  Offset M (4 bytes, 指向 Filter Data M 起点)       | |
|   |  Offset M+1 (4 bytes, 数组总长, 指向本数组起点)    | |  <-- 这个数组叫 Offset Array
|   |  ------------------------------------------------  | |
|   |  Offset Array Offset (4 bytes, 指向上述数组的起点) | |  <-- 解析时从后往前读
|   |  Base Lg (1 byte, 默认11，代表 2^11 = 2KB 分块)    | |  <-- 决定了多少数据生成一个Filter
|   +----------------------------------------------------+ |
|                                                          |
+----------------------------------------------------------+
|                                                          |
|                  MetaIndex Block                         |  (存放元数据的索引，通常只有一条记录)
|       Key: "filter.leveldb.BuiltinBloomFilter2"          |  (Value 是上面 Filter Block 的位置)
|                                                          |
+----------------------------------------------------------+
|                                                          |
|                    Index Block                           |  (存放所有 Data Block 的索引)
|       Key: "短 Key 1"  -> Value: Data Block 1 的位置     |
|       Key: "短 Key 2"  -> Value: Data Block 2 的位置     |
|                        ...                               |
|                                                          |
+==========================================================+
|                                                          |
|                      Footer                              |  (永远在最后，雷打不动的定长 48 字节)
|             1. MetaIndex Block 的句柄 (位置+大小)        |
|             2. Index Block 的句柄 (位置+大小)            |
|             3. Padding (补齐零)                          |
|             4. Magic Number (8字节魔数，校验文件合法性)  |
|                                                          |
+----------------------------------------------------------+  <-- 文件物理末尾 (EOF)
```

---

### 核心区块

**1. Data Blocks（用户数据区）**
* **内容**：你存入的真实 Key-Value，按 Key 严格从小到大排序，并做了前缀压缩。
* **大小**：解压前通常是 4KB 左右（由 `Options` 配置）。
* **尾巴**：每一个 Block 物理写完后，都会带着一个 **5 字节的 Trailer**（1 字节压缩类型 + 4 字节 CRC32 校验码）。

**2. Filter Block（布隆过滤器块）**
* **内容**：为了加速查询而生的“拦截网”。它把所有的 Data Block 按每 2KB 切分，为每一段生成一个布隆过滤器。
* **特点**：它是**一整个大块**，没有像 Data Block 那样拆分成多个小块，且**不开启压缩**。

**3. MetaIndex Block（元数据索引块）**
* **内容**：给高级功能（如过滤器）指路的“导航仪”。
* **格式**：它本身也是一个标准的 Block（有前缀压缩，有 5 字节尾巴）。里面的 Key 是功能名称（如 `"filter.xxx"`），Value 是对应功能块的 `BlockHandle`。

**4. Index Block（数据索引块）**
* **内容**：给所有 Data Block 指路的“总目录”。
* **格式**：也是一个标准的 Block。为了省空间，它的 Key 用的是两个 Data Block 之间的“最短分隔词”（Short Separator），Value 是对应的 Data Block 的 `BlockHandle`。

**5. Footer（文件基石）**
* **内容**：整个文件的入口点。存放着 MetaIndex Block 和 Index Block 的 `BlockHandle`，外加防伪魔数。
* **铁律**：永远固定在文件末尾，**死死占据最后 48 个字节**。

---

###  为什么这么排布？

你可能会问：为什么要按照 `数据 -> 过滤器 -> 索引 -> Footer` 这个顺序排？

因为**读取的时候，时间是倒流的！**
想象一下你要在一个 200MB 的 `.ldb` 文件里找一个叫 `"rocket"` 的 Key，LevelDB 是这样操作的（极度节省 I/O）：

1.  **盲读尾部**：不管文件多大，直接把文件指针拉到 `EOF - 48 字节`。瞬间读出 Footer，拿到魔数（确认没坏）和两个核心 Index 的位置。
2.  **读数据目录**：根据 Footer 提供的位置，去文件中精准读取 **Index Block**。在内存里对目录进行二分查找，发现 `"rocket"` 应该在第 50 个 Data Block 里（假设偏移量是 `200000`）。
3.  **读过滤器目录**：根据 Footer 提供的位置，去读 **MetaIndex Block**，找到布隆过滤器的位置。
4.  **按图索骥问保安**：算出第 50 个 Data Block 对应的过滤器索引（`200000 >> 11`），去 **Filter Block** 里拿出那几十个字节的位图。问：“`"rocket"` 在不在？”
5.  **精准打击**：如果过滤器说“不在”，直接返回 NotFound，**完美省下了一次几 KB 的磁盘读取！** 如果说“在”，才根据第 2 步拿到的位置，去磁盘把第 50 个 **Data Block** 读入内存，解压，查找，返回结果。

---

### 完整的 LOG 文件 (WAL) 物理布局图

```text
+=============================================================+  <-- 偏移量 0 (文件开头)
|                                                             |
|   +--- 物理 Block 1 (严格固定 32KB) ----------------------+ |
|   |                                                       | |
|   |  [Record 1 (FULL)]                                    | |
|   |   - Header: Checksum(4B) | Length(2B) | Type=1        | |  (存着一次小体积的 WriteBatch)
|   |   - Payload: Put("key1", "val1")                      | |
|   |                                                       | |
|   |  [Record 2 (FIRST)]                                   | |
|   |   - Header: Checksum(4B) | Length(2B) | Type=2        | |  (存着一次超大体积 WriteBatch 的开头部分)
|   |   - Payload: Put("big_key", "big_value_part_1")       | |
|   |                                                       | |
|   |  [Padding (废弃区)]                                   | |
|   |   - 如果当前块剩余空间 < 7 字节，连 Header 都放不下   | |  (直接填 \x00 废弃，开启下一个 Block)
|   +-------------------------------------------------------+ |
|                                                             |
+=============================================================+  <-- 偏移量 32768 (32KB)
|                                                             |
|   +--- 物理 Block 2 (严格固定 32KB) ----------------------+ |
|   |                                                       | |
|   |  [Record 2 (LAST)]                                    | |
|   |   - Header: Checksum(4B) | Length(2B) | Type=4        | |  (无缝接续上一个 Block，存完剩余数据)
|   |   - Payload: "big_value_part_2"                       | |
|   |                                                       | |
|   |  [Record 3 (FULL)]                                    | |
|   |   - Header: Checksum(4B) | Length(2B) | Type=1        | |  (又是一次普通的写入)
|   |   - Payload: Delete("key1")                           | |
|   +-------------------------------------------------------+ |
|                                                             |
+=============================================================+
|                        ...                                  |
+-------------------------------------------------------------+  <-- 文件物理末尾 (EOF)
```

#### 核心区块

**1. 物理 Block（底层承载块）**
* **内容**：日志文件的物理切分单位。
* **铁律**：永远是雷打不动的 **32KB**。不论你的数据多大或多小，底层都是被切成一个个 32KB 的格子。

**2. Record Header（记录头，7 字节）**
* **内容**：每一段数据的“身份证”。
* **格式**：`4 字节 CRC32 校验和` + `2 字节数据长度` + `1 字节记录类型`。
* **校验范围**：Checksum 保护的是 Type 和 Payload，确保数据没有因为磁盘静默损坏而变成乱码。

**3. Record Type（分片类型）**
* **FULL (1)**：完美！这条写入数据很小，一个 Block 完全装得下。
* **FIRST (2)**：数据太大了！当前 Block 装不下，这里只存第一部分。
* **MIDDLE (3)**：数据真的太大了！上个 Block 没存完，这个 Block 还是存不完，夹在中间。
* **LAST (4)**：终于存完了！这是跨块大数据的最后一部分。

#### 为什么这么排布？

你可能会问：为什么要强行切分成 32KB，还要搞这么复杂的分片（FIRST/MIDDLE/LAST）？直接一路追加写（Append）不行吗？

因为**要应对机器随时可能发生的“突然断电”！**

1.  **极速追加**：日志必须快，所以它没有任何索引，全是顺序追加写。
2.  **断电撕裂问题**：如果发生断电，磁盘往往只能保证扇区级（通常是 512 字节或 4KB）的原子写入。如果你写了一条 100KB 的数据，可能写到 18KB 时断电了，文件尾部全是错乱的垃圾数据。
3.  **精准截断止损**：重启恢复时，LevelDB 按照 32KB 的步长去读。如果读到一个 `FIRST` 记录，但下一个记录因为断电损坏了（Checksum 报错），或者下一个记录不是 `MIDDLE/LAST` 而是直接变成了 `FULL`（说明数据不完整），LevelDB 就能立刻判定：**“从这里开始遭遇了断电！”**。系统会精准地丢弃这条损坏的跨块记录，但**完美保留断电前所有健康的数据**。

---

### 完整的 MANIFEST 文件物理布局图

```text
+==========================================================+  <-- 偏移量 0 (文件开头)
|                                                          |
|   +--- 物理 Block 1 (严格固定 32KB，和 LOG 完全一样) --+ |
|   |                                                    | |
|   |  [Record 1 (FULL): Base Snapshot 基础快照]         | |  (DB 启动/轮转时，打下的全局底稿)
|   |   - Header: 7 字节 (同上)                          | |
|   |   - Payload (序列化后的 VersionEdit):              | |
|   |       > Comparator: "leveldb.BytewiseComparator"   | |  (比较器名字，防止乱套)
|   |       > LogNum: 15                                 | |  (当前使用的日志文件编号)
|   |       > NextFileNum: 20                            | |
|   |       > LastSeq: 100050                            | |
|   |       > AddedFiles: L0=[001.sst], L1=[002.sst]     | |  (登记当前存在的所有文件！)
|   +----------------------------------------------------+ |
|                                                          |
+==========================================================+
|                                                          |
|   +--- 物理 Block 2...                                 | |
|   |                                                    | |
|   |  [Record 2 (FULL): Incremental Edit 增量变更]      | |  (发生了一次 Minor Compaction)
|   |   - Header: 7 字节 (同上)                          | |
|   |   - Payload (序列化后的 VersionEdit):              | |
|   |       > LogNum: 16                                 | |  (换了新日志)
|   |       > NextFileNum: 21                            | |
|   |       > LastSeq: 100080                            | |
|   |       > AddedFiles: L0=[020.sst]                   | |  (新增了一个 L0 表)
|   |                                                    | |
|   |  [Record 3 (FULL): Incremental Edit 增量变更]      | |  (发生了一次 Major Compaction)
|   |   - Payload:                                       | |
|   |       > DeletedFiles: L0=[020.sst], L1=[002.sst]   | |  (合并时删除了旧表)
|   |       > AddedFiles: L1=[021.sst]                   | |  (合并生成了新表)
|   +----------------------------------------------------+ |
|                                                          |
+==========================================================+
|                        ...                               |
+----------------------------------------------------------+  <-- 文件物理末尾 (EOF)
```

#### 核心区块

**1. 物理层外壳（Log Writer）**
* **内容**：MANIFEST 的底层写入代码和 LOG 文件用的是**同一个类**（`log::Writer`）。所以它同样有 32KB 的物理块、7 字节的 Header 和分片机制。

**2. Base Snapshot（基础快照记录）**
* **内容**：MANIFEST 文件的**第一条（或前几条）记录**。
* **特点**：它是一个极其庞大的 `VersionEdit`，里面记录了当时数据库所有的状态（所有的 SST 文件、最新的序列号等），相当于一个“出厂初始设置”。

**3. Incremental Edit（增量变更记录）**
* **内容**：后续追加的所有记录。
* **特点**：极其轻量。每次后台合并（Compaction）或者 MemTable 刷盘，只会记录**变化的部分**（谁被删了，谁被加了，序列号涨到了多少）。

#### 为什么这么排布？

你可能会问：为什么要用“写日志”的方式来管理元数据？为什么不直接在磁盘上维护一个 JSON 或者 SQLite 数据库，每次改里面的字段？

因为这实际上是一个**“状态机回放（State Machine Replay）”**的精妙设计！

1.  **极度安全与高效**：如果直接修改同一个文件，一旦修改中途断电，整个数据库的“大脑”就报废了。而**追加写（Append）是磁盘最安全、最快的操作**。写成功了就算更新，写了一半断电，大不了丢弃最后一次未完成的变更。
2.  **电影放映式读取**：读取 MANIFEST 时，**时间是顺流而下的**。LevelDB 启动时是这样做的：
    * 读取 `Record 1 (Base Snapshot)`，在内存里建立一个初始的 `Version`（版本 A）。
    * 读取 `Record 2 (增量)`，应用到版本 A 上，得到 `Version`（版本 B）。
    * 读取 `Record 3 (增量)`，应用到版本 B 上，得到 `Version`（版本 C）。
    * 一路回放到文件末尾，最终在内存中构建出当前数据库最真实、准确的 `Current Version`。
3.  **防止无限膨胀**：如果一直追加，MANIFEST 会不会有 100 个 G？不会。当它大到一定程度（比如几兆），LevelDB 会直接触发一次轮转（Rotate）：把内存里当前的最终状态，作为 `Base Snapshot` 写入一个新的 MANIFEST 文件中，然后直接删掉旧的 MANIFEST 文件。轻装上阵！