### 记录
使用Asan进行排查bug，发现内存泄漏
+ 原因1 在version的析构函数中，忘记调用ref--，使得filemetadata仍有引用计数没有销毁。
+ 原因2 在DB的基类中缺少virtual的虚构函数，导致DBImpl只绑定了DB的基类函数。
+ 原因3 在logger的初始化中，对于配置的处理除了问题，导致一直判定为false,不会执行delete。

程序访问越界内存
+ 原因 在tablebuidler中最后写入压缩类型和校验码时，使用了变长编码，导致原来分配好的5字节不够用了，
修改为定长编码，解决问题。